---
  - name: Cleanup /tmp directory (inside docker containers)
    hosts: all
    gather_facts: False

    tasks:
      - name: get docker containers
        docker_host_info:
          containers: yes
        register: result

      - name: show output
        debug:
          var: result.host_info
